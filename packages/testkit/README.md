# Testkit package

This package will own deterministic election simulation, malicious transcript
fixtures, and reusable integration scaffolding.

The current release only establishes the package boundary and build shell.
