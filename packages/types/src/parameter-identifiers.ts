/** Target-bound share-selection parameters required before decryption shares are accepted. */
export const targetBoundShareSelectionId =
    'target-bound-first-valid-share-selection-v1';

/** Target decryption parameters required by accepted manifests. */
export const targetDecryptionId = 'BGV-RNS-AsyncTargetDecryption-v1';

/** Direct encrypted ballot layout required by accepted manifests. */
export const encryptedBallotLayoutId = 'direct-encrypted-ballot-layout-v1';

/** Direct ballot validity proof identifier required by accepted manifests. */
export const ballotValidityProofId =
    'direct-encrypted-ballot-validity-proof-v1';

/** Direct encrypted ballot aggregate identifier required by accepted manifests. */
export const encryptedBallotAggregateId =
    'direct-encrypted-ballot-aggregate-v1';

/** Mandatory evaluator replay identifier required by accepted manifests. */
export const evaluatorReplayId = 'direct-encrypted-ballot-evaluator-replay-v1';

/** Direct encrypted comparison identifier required by accepted manifests. */
export const directComparisonId = 'direct-encrypted-ballot-comparison-v1';

/** Mobile runtime identifier required by accepted manifests. */
export const mobileRuntimeId = 'mobile-flagship-runtime-v1';

/** Ballot score encoding identifier for direct encrypted score coordinates. */
export const ballotScoreEncodingId = 'DirectEncryptedScoreSlots-v1';
