# Crypto package

This package will own provider selection, runtime capability checks, and
domain-separated cryptographic wrappers.

The current release only establishes the package boundary and build shell.
