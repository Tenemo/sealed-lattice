# WASM package

This package owns Rust/WASM loading, typed wrappers, and runtime-specific instantiation details.

The current release ships the transcript-core command contract around the Rust crate. It verifies canonical objects, fixture replays, chunk roots, reserved protocol hash derivation, `GF(65537)` interpolation/comparison checks, development passive BGV setup package verification, the initial accepted collective BGV setup verifier gate with carry-aware VSS relation profile binding, BDLOP/LNP-style setup commitment profile binding, root-bound review-gated setup commitment security certificate verification, setup-proof profile and challenge-domain binding, fixed `d = 128`, `omega = 2` LNP tbox challenge layout metadata, strict LaZer-style tbox proof-byte decoding with retained decoded transcript fields for the setup-proof profile, full-profile public VSS material size accounting, root-bound setup transport certificate and chunk-manifest verification, accepted HE security certificate verification for current `Q_data`/`Q_share` direct evaluator replay exposure with explicit `Q_target` refusal, transported binary public VSS material derivation for threshold-share commitments, ML-DSA-65 phase signed-root verification, common-randomness commit/reveal binding, accepted public-derivation binding, deterministic setup public-matrix coefficient sample binding for commitment and setup-proof matrices, root-bound VSS coefficient commitment record verification, root-bound public VSS coefficient commitment material verification with profile-ring enforcement, root-bound private VSS envelope commitment-set verification with mailbox AAD, recipient mailbox public-key bytes-hash binding, and hashed encrypted-envelope object binding, root-bound signed VSS share acceptance verification, signed VSS complaint abort verification, local private VSS share envelope refusal for leaked coefficient material, plaintext aggregate openings, and plaintext carry witnesses, proof-shaped private VSS share proof hash binding with proof-verifier-missing refusal, accepted-package threshold-share commitment recomputation from full public VSS coefficient commitment material, root-bound same-secret statement and proof-family binding verification, optional review-gated same-secret LNP proof-envelope verification with pinned tbox parameter profile binding, tbox commitment-prefix binding, LNP-derived scalar challenge binding, `SameSecretProofFamilyBindingRoot` binding, root-bound transported proof material, and fixed verifier-side response bounds over embedded or transported public VSS material, root-bound public-key share/proof statement verification, optional review-gated public-key LNP proof-envelope verification with pinned tbox parameter profile binding, tbox commitment-prefix binding, LNP-derived scalar challenge binding, root-bound transported proof material, verified same-secret proof-root and proof-family-root binding, no-wrap carry checks, and fixed verifier-side response bounds over embedded or transported public share material, review-gated collective public-key aggregate verification from LNP proof-bearing shares with same-secret proof-set, same-secret proof-family, and public-key LNP proof-set binding, premature raw or root-only collective public-key material refusal, frozen evaluator-key schedule verification, root-bound relinearization and Galois proof-container verification with same-secret family-root, malformed aggregate, and schedule drift refusal, premature evaluation-key material refusal, roots-only local trustee setup state commitment verification without aggregate-opening roots, neutral accepted-setup command error mapping, the internal direct encrypted ballot command, stable canonical rejection codes, and explicit WASM kernel integrity expectations in Node and browser/WASM builds.

The WASM package is not a public voting API. Raw BGV operations, proof witnesses, encryption randomness, plaintext oracle helpers, evaluator intermediate openings, raw VSS shares, setup secrets, and decryption helpers remain internal. The passive setup command is development evidence only and cannot close `CollectiveBgvSetup-v1`.
