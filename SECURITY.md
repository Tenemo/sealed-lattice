# Security policy

`sealed-lattice` is an unaudited research prototype. The open issues below prohibit real elections and other security-sensitive use. Use synthetic data only.

## Report a vulnerability

Use GitHub private vulnerability reporting when available. If it is unavailable, open a minimal public issue requesting a private contact path without including exploit details.

Include:

- the affected package version or commit;
- a minimal reproduction;
- the expected and observed behavior; and
- whether private material may have been exposed.

Do not attach real election data or unpublished exploit material. Do not attach private keys, ballots, shares, or witnesses.

## Supported profile

No released version is supported for production use. The completion and qualification target is one scalar package with ten roster participants and ten options. For that profile, the protocol formulas derive an active corruption bound of three, a reconstruction threshold of four, and a direct finality quorum of eight. The finality intersection additionally tolerates one independently rolled-back or lost honest finality lock. There is no separate ballot-set or network state-witness quorum.

Schemas and tally-circuit compilers admit rosters and option counts from 3 to 20 and 2 to 20 respectively, but other profiles are structural inputs only. They carry no cryptographic, runtime, or support claim.

The design aims for at least 80 bits of modeled attack work against quantum-capable adversaries under its stated assumptions and limits. That claim has not been established end to end and is not a NIST category, production rating, audit, or certification.

Cryptographic admission and supported-phone qualification are independent results for the same exact bytes. Both are incomplete. Only release Chrome on the selected physical phone can provide supported-phone evidence.

Here, a poll is the user-facing workflow. An action is one canonical protocol execution for that poll.

## Security model

- The protocol protects ballot scores, not voter anonymity. The public information is exactly the frozen roster, each signed submit-or-abstain declaration, whether each submitted ballot was usable, whether any usable ballot exists, and the result. Honest clients submit only usable ballots, so the usability bit is constant for every honest submitter and reveals nothing about honest scores; for a corrupt submitter it attributes rejection to that participant's own signed source.
- The host-application duties described in the [README](README.md#library-boundary) grant no protocol authority. Participants confirm and freeze the roster. Identity vetting and Sybil resistance remain outside the library, and the organizer remains an ordinary roster participant.
- Every roster participant contributes to tally preparation. No smaller group, dealer, server, or organizer can complete it.
- The adversary statically corrupts at most three participants and may rush, equivocate, withhold, replay, reorder, fork, or replace messages. A separate passive-exposure game covers at most three disclosed shares; the two bounds are not combined.
- Transcript, mailbox, and storage services are untrusted. Silence never means abstention. Missing required input leaves the action pending.
- Acceptance comes only from positive verification. The responsible verifier recomputes context and source bindings, then checks the canonical bytes and every required authentication, opening, state, and construction relation. Producer status and caller-selected targets cannot authorize acceptance; neither can signatures, raw shares, or fixtures on their own.
- The selected ballot set is derived from a roster-complete declaration and availability inventory. Every finality signer recomputes it; no separate quorum can choose another subset or authorize an omitted-input opening.
- Only roster participants using their own clients may establish direct target finality or release verified ballot inputs and evaluation messages after finality.
- Participant action state is bound to one browser profile. There is no backup, export, migration, or replacement-device continuation. Lost or unverifiable state retires that participant from the affected action.
- Long operations require authenticated checkpoints at deterministic safe boundaries. Correctness cannot depend on wake locks, hidden-page execution, lifecycle callbacks, or final worker notification.
- Cryptographic randomness comes from browser-local platform randomness and is durably retained before publication. Byte-identical resume reuses the retained value; deterministic action derivation is not a freshness or rollback proof.
- Every required participant operation remains available through scalar-capable, single-worker mobile-browser WebAssembly without native helpers or stronger-device exceptions.

These properties assume honest delivered application code while secrets are handled, uncompromised honest devices, an accepted participant-confirmed frozen roster, and closure of the issues below.

## Open security issues

These are layered blockers: construction, integration, supported-phone qualification, and activation must all pass independently.

- `SEC-001`: No independent audit, certification, production hardening, or production approval exists.
- `SEC-002`: The rejected public-semantic-map and action-wide-continuation experiment remains removed. The separate historical sixteen-row source module, parser, generator, evaluator, and relation-specific tests are also deleted; commands `34..40` remain refusal tombstones. The version-one padded vertical executes the ten-participant, ten-option tally for every `topCount` through Rust, scalar WebAssembly, the worker, segmented durable state, and verified result or no result. It uses no refreshed-wire semantic map or action-wide continuation pair. Its 256- and 320-bit operation keys do not satisfy the available ideal-permutation KMAC theorem's key-length premise, so the leading candidate instead names exact direct fixed-KMAC256 games for independent-label row hiding, conditioned alternative-key hiding, and known-key continuation second-preimage resistance. Those assumptions have not been independently accepted or composed with the source, preparation, primitive, state, and static-quantum games. The 192-byte theorem projection remains dormant reopening evidence, not an emitted replacement. No candidate has passed cryptographic admission, release-browser lifecycle closure, or selected-phone qualification.
- `SEC-003`: The version-one worker validates one retained ML-DSA-65 signing key and one retained ML-KEM-768 decapsulation key against the canonical roster on every load and persists exact preparation, source, finality, generation, evaluation, and terminal state before release. Segmented generation and evaluation use object kinds ten and eleven with version-six action journals. Every mutation authenticates the root and exact seven-store protected-record inventory, advances the inventory generation, fresh-reseals every retained envelope, and atomically exact-compares and replaces the whole predecessor set. An insertion, mixed epoch, single-record deletion or rollback, and every proper-subset root/action/source/generation/evaluation rollback fail before another signature, decapsulation, entropy allocation, or result. Source and generation records contain private scores, both label alternatives, and continuation keys, so the conditional theorem applies explicit local-HMAC and local-AEAD shadow games before source or operation simulation: storage-visible honest envelopes encrypt equal-length zeros while exact restore uses challenger-only plaintext only after inventory authentication and state-policy validation. These are assumptions, not consequences of encryption tests. Replay of one complete old root and every matching protected envelope, and total erasure of the initialized root and all seven protected stores to their pristine empty state, remain environmental continuity exclusions. The latter is indistinguishable from first initialization to the current worker. The retained-state premise also excludes synthesis, replacement, or transplantation of an attacker-controlled usable HMAC `CryptoKey` into an honest root record: the worker validates a stored key's type and usages but has no external root-identity pin. Clean development-Chromium evidence now exercises the full reseal schedule, and the clean external-Chrome proxy binds the current resource inputs; quota pressure during the complete ceremony, eviction, cancellation, hostile-relay operation, external release-browser lifecycle behavior, and complete cleanup remain incomplete.
- `SEC-004`: No cryptographic suite is activated. The public package exposes foundation operations only, its separately built WebAssembly artifact has no construction dispatcher, and component tests cannot authorize production dispatch.
- `SEC-006`: The live worker now carries the internal capability chain from complete preparation through full-score source fixation, self-contained target finality, segmented activation, segmented evaluation, and the owning result/no-result verifier. Finality binds the exact action, roster, compiler, circuit, output width, source set, predecessor, activation, and direct-finality identities. Its verifier parses and validates the supplied canonical ML-DSA/ML-KEM roster, recomputes its identity, compares it with the target, and only then verifies the eight-of-ten carrier set. Downstream methods consume typed carrier-independent capabilities rather than ambient worker agreement. The chain remains internal, unadmitted, and absent from the public foundation-only WebAssembly artifact; there is no admitted public end-to-end capability chain.
- `SEC-007`: The internal vertical implements full-score source fixation, submit or abstain, unusable-ballot handling, selected-set derivation, and positive-verifier integration for all 400 private score bits. A conditional theorem maps source privacy, corrupt extraction before the ideal result query, joint losing-fork simulation, malicious selective private delivery, authenticated local-custody shadow state, and compiler-topological static-quantum composition to exact ideal-stream lemmas or named direct assumptions. The reduction hides every producer ciphertext before using its inactive token as a descendant challenge key, removes real protected-record plaintexts from later auxiliary views, and applies alternative-key hiding only to honest receivers; corrupt receiver coordinates remain adversarial confluence inputs. It does not inherit security from either the rejected evaluation layer or the passing full-tally implementation, and independent byte-level and proof review remain prerequisites for admission.
- `SEC-008`: No supported-phone qualification has completed every participant operation in release Chrome on the selected physical phone for the exact scalar package.
- `SEC-017`: Browser-local root-key custody and derivation-count continuity are not closed across the complete cold-start, resume, retirement, and cleanup lifecycle.
- `SEC-019`: No immutable evidence bundle binds the selected construction, reductions, production counters, source, dependencies, and release package bytes.
- `SEC-020`: The replacement has authenticated segmented generation and evaluation checkpoints with byte-identical cold restore in native and development-Chromium ceremonies. Forced termination after initialization, chunk persistence, signed activation publication, evaluation initialization, evaluation-step persistence, and terminal persistence replays or resumes only retained bytes. Exact-inventory tests also reject single-record deletion, insertion, unsigned-source subset rollback, and action/generation or action/evaluation subset rollback. This establishes development lifecycle correspondence, not checkpoint noninterference in the full security proof, complete-old-inventory rollback detection, total local-database erasure detection, maximum lost work under release-browser interruption, quota behavior, or phone evidence.
- `SEC-021`: The clean synthetic external release-Chrome run at `logs/2026-09-03/2026-09-03T00-53-58.980Z-evidence-external-chrome-resource-screen` wrote, reread, and SHAKE-verified the generated ten-submitter maximum with strict-durability IndexedDB transactions, then showed an absent database and zero origin usage after closing and reopening the identical profile. Its result binds clean commit `22d2b4a8`, the exact harness and configuration sources, package inputs, and both WebAssembly binaries by digest and length. This closes only the isolated quota-admission, integrity, ordinary cleanup, scalar-KMAC, process-memory, and restart-reclamation screens. No complete browser-storage evidence set yet covers transaction overlap, amplification, quota pressure, eviction, cancellation, action-wide mutation, repair, and cleanup inside the one-participant-at-a-time hostile ceremony.
- `SEC-023`: The rejected emitted full tally published a zero-label semantic bit for each local label pair. Because the evaluator also received the active label, it could decode every intermediate coordinate and interpolate private conjunction values. This deterministic classical transcript attack invalidated the experiment's former conditional one-AND and full-circuit privacy arguments without breaking its PKE, hashes, AEAD, labels, or state. The exact evaluation instantiation is rejected and removed. Its former command range and durable object kinds are reserved tombstones, and old affine-bearing preparation and source records are not accepted under the replacement record versions.
- `SEC-024`: The leading version-one candidate has emitted-object transfer, storage, checkpoint, worker-copy, WebAssembly-memory, scalar-work, visit, and foreground measurements for complete top-one and top-ten result ceremonies. Participant-streamed evaluation leaves signed chunks and durable checkpoints unchanged and keeps every admitted width within the current request target. The clean maximum-width development-Chromium result, including the full authenticated-inventory reseal schedule, is owned by `logs/2026-09-03/2026-09-03T01-01-02.000Z-evidence-padded-tally-top-count-10`. Its five recorded loads begin at roster confirmation and omit join; the corrected join-counted graph has an ordinary maximum of six for earlier joiners and five for the last joiner, an all-abstain maximum of five, and a defined three-crash-plus-repair maximum of ten. The clean mobile-emulation proxy and its exact KMAC histogram, corpus, reclamation, configuration, and operating-system process-memory measurements are owned by `logs/2026-09-03/2026-09-03T00-53-58.980Z-evidence-external-chrome-resource-screen`. The rejected 192-byte reopening projection remains formula-only evidence and does not define a fallback. These runs are development evidence only; one-participant-at-a-time cold-page hostile-relay operation, quota pressure, eviction, cancellation, complete cleanup, full external release-Chrome closure, and selected-phone feasibility remain open.
- `SEC-027`: The removed randomized-response flow would expose its protected source if complementary responses were obtainable under one pad. A later exact-history audit found that its accepted-opening relation did not make those opposite responses reachable, so the earlier finite cases are not evidence of an executed historical exploit. The route remains rejected and removed because it lacks a complete theorem and is not authorized as a fallback. The later semantic-map direction also has a full internal scalar-WebAssembly vertical but is rejected by `SEC-023`; neither route is a fallback.
- `SEC-028`: The removed evaluator reused one affine continuation pair for every conjunction in an action. Its public gate polynomials satisfy `H_g=A+Y_gB`; any two distinct public `Y` values recover `B` by exact polynomial division and then recover `A`, even if their selector constants are equal. Observing both selector values is a simpler corollary. Including the gate ordinal in the hash input separated ciphertext domains but did not make the underlying keys gate fresh. Any retained continuation design needs operation-fresh secret material and a complete serial, fan-out, multi-output, and full-tally privacy theorem.
- `SEC-029`: The removed implementation derived all garbling labels from one 32-byte activation seed while its working theorem assumed independent labels. The leading candidate closes that theorem-to-bytes mismatch by requesting every 320-bit label and point bit directly from browser platform randomness, rejecting equal pairs, and durably checkpointing the allocation before publication. It publishes no seed or label-corpus digest. Browser-CSPRNG security, durable generation chronology, collision conditioning, and the fixed-KMAC hidden-key games remain explicit admission terms; direct sampling does not prove them.
- `SEC-031`: The removed operation-fresh replacement split each affine translation into independently label-masked bit-basis rows. The evaluator could decrypt every selected row with the corresponding public active label. Four masked coordinates outside `{0,1}` exposed enough degree-three affine evaluations to recover the opposite continuation key at the same gate and decrypt both refreshed-label alternatives. This is a classical transcript attack; signatures, commitments, fresh keys, independent labels, and wrong-key tags do not repair it. Any future selected-label relation must expose only its selected aggregate and pass same-gate public-transcript extraction review before composition.
- `SEC-032`: The padded relation masks each selected basis value with an independent pairwise-derived module value, derives continuation material separately for every gate and receiver, and publishes no label-corpus digest or refreshed-wire map. The leading candidate samples direct 320-bit labels and emits the complete tally through production Rust, scalar WebAssembly, the worker, segmented durable generation and evaluation, and the terminal verifier for every `topCount`. An independent compiler and serialized-byte parser reproduces the full circuit, chunk plan, relation inventory, and result. Native and development-browser hostile tests cover serial multiplication, fan-out, reconvergence, multiple outputs, both selectors, corrupt rows and continuations, three malicious participants, forks, withholding, malformed encryption, replay, rollback and state loss that leave local evidence, abstention, unusable ballots, and all abstention. This remains implementation, correspondence, confluence-case, and resource evidence. Cryptographic admission now depends on independent acceptance of the exact direct fixed-KMAC assumption and closure of the remaining games, not on a production re-emission at 1,536 bits.
- `SEC-033`: The independent KMAC audit fixes KMAC256's rate at 1,088 bits and confirms that the available outer-keyed-sponge theorem does not cover the leading candidate's 256- and 320-bit keys or fixed public Keccak-f[1600]. The maximum-width all-honest graph samples 2,892,680 direct labels; a fixed trace has 1,446,340 inactive labels, 1,439,220 nonempty direct challenge keys, 5,948,240 registered challenge outputs, and maximum fan-out 332. `A_KMAC-DIRECT` applies only after the producer ciphertext for a challenge label is hidden. `A_KMAC-ALT` has at most 29,620 honest-receiver counterfactual targets after their gate-specific conditioned-freshness reductions, while `A_KMAC-2P` covers known-key second-preimage attempts against honest-receiver forty-byte continuation authenticators. Counts scale with the fixed honest receiver set; corrupt receiver rows and keys are not reclassified as hidden. The complete emitted graph still has 11,955,720 generation calls and 3,596,140 selected-evaluation calls. Adversarial coherent queries, honest emitted work, online verifier candidates, offline candidate tests, and action/session multiplicities are separate counters. There is no bare `2^-320` continuation theorem, no inventory limit obtained by dividing the adversarial budget by honest evaluation cost, and no aggregate security-bit field. The random-function/QROM expression is heuristic only, and the fixed-AES game explicitly absorbs permutation-to-function switching. These are direct assumptions and conditional proof obligations, not admitted security, until independent review accepts every emitted-byte mapping. The dormant 1,536-bit projection is rejected reopening evidence: it would obtain 81.735 bits after its ideal-permutation key and action unions but would not establish the fixed-function premise.

Identifiers are stable and are not reused. `SEC-005`, `SEC-009` through `SEC-016`, `SEC-018`, `SEC-022`, `SEC-025`, `SEC-026`, and `SEC-030` are retired.

## Outside the security model

A compromised participant device holds that participant's keys and authority. It can disclose local secrets and send arbitrary messages. The following remain outside the security boundary:

- compromise beyond the active fault bound;
- data already present on a compromised device;
- malicious same-origin application code or platform key storage;
- adaptive-corruption security and post-action compromise security;
- everlasting secrecy, receipt freeness, coercion resistance, and endpoint security;
- denial-of-service resistance, guaranteed availability, or guaranteed output; and
- side-channel resistance, including timing, traffic-analysis, power, cache, and speculative-execution attacks.

Logical deletion and secret-buffer zeroization are required hygiene, but browser storage cannot attest physical erasure. Physical reclamation is measured for storage feasibility, not claimed as post-compromise secrecy.

Protocol safety instead relies on the accepted roster, positive verification, one-shot state, exact target binding, and the stated cryptographic and endpoint assumptions. See the [README](README.md) for the current implementation boundary.
