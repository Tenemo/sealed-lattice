[workspace]
members = [
    "crates/sealed-lattice-kernel",
    "crates/sealed-lattice-sphincs-plus",
]
resolver = "3"

[profile.test]
opt-level = 3
overflow-checks = true

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = 3
panic = "abort"
