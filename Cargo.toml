[workspace]
members = [
    "crates/sealed-lattice-kernel",
    "tools/process-memory-guard",
]
exclude = ["fuzz"]
resolver = "3"

[profile.test]
opt-level = 3
overflow-checks = true

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = 3
panic = "abort"
